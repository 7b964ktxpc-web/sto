'use client';
import { useEffect, useState } from 'react';
type Analytics={bookings:number;completed:number;cancelled:number;revenue:number;registrations:number;activeUsers:number;conversion:number};
export default function AdminAnalyticsPage(){const [data,setData]=useState<Analytics|null>(null);const [error,setError]=useState('');useEffect(()=>{fetch('/api/admin/analytics').then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||'Нет доступа');setData(x.analytics)}).catch(e=>setError(e instanceof Error?e.message:'Не удалось загрузить аналитику'))},[]);return <main className="page"><header className="hero"><div className="hero-inner"><span className="eyebrow">STO NSK · Analytics</span><h1>Аналитика платформы</h1><p>Ключевые показатели текущего месяца.</p></div></header><div className="content">{error?<div className="card error">{error}</div>:!data?<div className="card">Считаем показатели…</div>:<><div className="feature-grid"><div className="feature"><strong>Записи</strong><span style={{fontSize:28,fontWeight:800}}>{data.bookings}</span></div><div className="feature"><strong>Завершено</strong><span style={{fontSize:28,fontWeight:800}}>{data.completed}</span></div><div className="feature"><strong>Отменено</strong><span style={{fontSize:28,fontWeight:800}}>{data.cancelled}</span></div><div className="feature"><strong>Регистрации</strong><span style={{fontSize:28,fontWeight:800}}>{data.registrations}</span></div><div className="feature"><strong>Активные пользователи</strong><span style={{fontSize:28,fontWeight:800}}>{data.activeUsers}</span></div><div className="feature"><strong>Конверсия в завершение</strong><span style={{fontSize:28,fontWeight:800}}>{data.conversion}%</span></div></div><div className="card" style={{marginTop:16}}><h2>Выручка</h2><div style={{fontSize:42,fontWeight:850}}>{data.revenue.toLocaleString('ru-RU')} ₽</div><div className="muted">Только оплаченные платежи за текущий месяц.</div></div></>}</div></main>}
